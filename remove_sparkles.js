const fs = require("fs"); const path = require("path"); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + "/" + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith(".jsx")) results.push(file); } }); return results; } const files = walk("Frontend/src"); files.forEach(file => { let content = fs.readFileSync(file, "utf8"); if (content.includes("Sparkles")) { content = content.replace(/<Sparkles[^>]*(\/>|>.*<\/Sparkles>)/g, ""); content = content.replace(/,\s*Sparkles\b/g, ""); content = content.replace(/\bSparkles\s*,/g, ""); content = content.replace(/{\s*Sparkles\s*}/g, ""); content = content.replace(/import\s*{\s*}\s*from\s*.lucide-react.[^;]*;/g, ""); fs.writeFileSync(file, content); console.log("Updated: " + file); } });
