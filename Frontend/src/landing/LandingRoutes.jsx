import React from 'react';
import LandingPage from './LandingPage';

const LandingRoutes = () => {
  // Simple routing just for the landing page scope if needed.
  // Currently handles the root path.
  return <LandingPage />;
};

export default LandingRoutes;
