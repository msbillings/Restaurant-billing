"# Direct IP Printer Integration\n\nThis plan implements a direct background printing system. It bypasses the macOS print driver and Chrome print dialog entirely by sending raw ESC/POS commands directly from the Node.js backend to your thermal printer over Wi-Fi.\n\n## Problem Addressed\nChrome strictly enforces fixed paper sizes defined by the macOS printer driver. Because your driver only exposes fixed lengths (e.g., 72x140mm), Chrome physically chops long receipts in half. \n\n## Proposed Changes\n\n### Backend\n#### [NEW] `utils/printer.js`\n- Create a utility that uses Node's built-in `net` module to establish a TCP socket connection to your printer's IP address (`192.168.29.242` on port `9100`).\n- Translate the receipt details into raw ESC/POS commands. This allows us to precisely control text formatting (bold, center) and, most importantly, send a **Hardware Cut Command** at the exact end of the receipt.\n\n#### [MODIFY] `routes/billRoutes.js`\n- Add a new route: `POST /api/bills/:id/print`\n- This route will receive the bill ID and your restaurant settings (Name, Address) from the frontend, fetch the bill, and trigger the `printer.js` utility.\n\n#### [MODIFY] `.env`\n- Add `PRINTER_IP=192.168.29.242`\n- Add `PRINTER_PORT=9100`\n\n### Frontend\n#### [MODIFY] `src/components/Invoice.jsx`\n- Update the **Print** button. \n- Instead of opening the browser's print dialog (`window.print()`), it will send a silent background request to the new backend print route.\n- Show a quick \"Printing...\" success message so you know it worked.\n\n## User Review Required\n\n> [!IMPORTANT]\n> Because this bypasses the browser entirely, the receipt will be printed in a standard text-based POS format rather than using browser fonts. It is extremely fast and professional, but the exact font will be the one built directly into your printer's hardware.\n\n## Verification Plan\n1. Apply the code changes.\n2. Restart the backend to load the new IP configuration.\n3. You will click \"Print\" on a bill. The receipt should instantl
<truncated 129 bytes>
"# Adobe-Style Desktop App & Auto-Update Architecture\n\nThis plan outlines how we will convert your current web-based billing system into a professional, installable Desktop Application with an automatic Over-The-Air (OTA) update system—exactly like Adobe or Spotify.\n\n## The Architecture\nTo achieve this, we will wrap your existing code in **Electron**. \n1. **The Desktop Wrapper:** Electron will bundle your Node.js backend and React frontend into a single `RestoPOS.app` (for Mac) or `RestoPOS.exe` (for Windows).\n2. **The Update Server:** We will use **GitHub Releases** (which is free) as your cloud update server.\n3. **The Update Flow (The \"Adobe\" Way):**\n   - Every time you open the RestoPOS app on your computer, the app quietly checks the cloud server in the background.\n   - If I have pushed a new version (like v1.0.2), the app detects it via internet.\n   - It downloads the update in the background while you continue billing customers.\n   - Once downloaded, it will show a small notification: *\"A new update is ready. Restart to install.\"*\n\n## Proposed Changes\n\n### Phase 1: Electron Integration\n- Create a root `main.js` Electron entry point.\n- Configure Electron to quietly start your Node.js backend on an available port, and then open the React frontend in a beautiful, full-screen native window without browser borders.\n\n### Phase 2: Direct POS Printing\n- Since we are now a native Desktop app, we will implement the direct TCP printing we discussed earlier.\n- The Electron backend will talk directly to your printer at `192.168.29.242`, ensuring instant, perfectly cut receipts every single time.\n\n### Phase 3: Auto-Update System\n- Install `electron-builder` and `electron-updater`.\n- Write the auto-update logic in `main.js`:\n  ```javascript\n  const { autoUpdater } = require(\"electron-updater\");\n  \n  // When app starts, check for updates\n  app.on('ready', () => {\n    autoUpdater.checkForUpdatesAndNotify();\n  });\n  ```\n- Configure the build pipeline so that whenever we want to rele
<truncated 1152 bytes>
"# Adobe-Style Desktop App & Auto-Update Architecture\n\nThis plan outlines how we will convert your current web-based billing system into a professional, installable Desktop Application with an automatic Over-The-Air (OTA) update system—exactly like Adobe or Spotify.\n\n## The Architecture\nTo achieve this, we will wrap your existing code in **Electron**. \n1. **The Desktop Wrapper:** Electron will bundle your Node.js backend and React frontend into a single `RestoPOS.app` (for Mac) or `RestoPOS.exe` (for Windows).\n2. **The Update Server:** We will use **GitHub Releases** (which is free) as your cloud update server.\n3. **The Update Flow (The \"Adobe\" Way):**\n   - Every time you open the RestoPOS app on your computer, the app quietly checks the cloud server in the background.\n   - If I have pushed a new version (like v1.0.2), the app detects it via internet.\n   - It downloads the update in the background while you continue billing customers.\n   - Once downloaded, it will show a small notification: *\"A new update is ready. Restart to install.\"*\n\n## Proposed Changes\n\n### Phase 1: Electron Integration\n- Create a root `main.js` Electron entry point.\n- Configure Electron to quietly start your Node.js backend on an available port, and then open the React frontend in a beautiful, full-screen native window without browser borders.\n\n### Phase 2: Direct POS Printing\n- Since we are now a native Desktop app, we will implement the direct TCP printing we discussed earlier.\n- The Electron backend will talk directly to your printer at `192.168.29.242`, ensuring instant, perfectly cut receipts every single time.\n\n### Phase 3: Auto-Update System\n- Install `electron-builder` and `electron-updater`.\n- Write the auto-update logic in `main.js`:\n  ```javascript\n  const { autoUpdater } = require(\"electron-updater\");\n  \n  // When app starts, check for updates\n  app.on('ready', () => {\n    autoUpdater.checkForUpdatesAndNotify();\n  });\n  ```\n- Configure the build pipeline so that whenever we want to rele
<truncated 1152 bytes>
"# RestoPOS Business Model & Architecture\n\nThis plan outlines the complete architecture to turn your software into a **downloadable, sellable product** with offline capabilities, license keys, and auto-updates—just like Adobe software.\n\n## Is it possible?\n**YES!** This is the exact industry standard for modern B2B desktop software. \n\n## The Grand Architecture\n\nTo achieve your vision, we will split your software into three main components:\n\n### 1. The Website & Cloud Licensing Server (Online)\nThis is a lightweight server hosted on the internet (e.g., Vercel, AWS).\n- **Purchasing:** Customers visit your website, pay for the software, and checkout.\n- **License Generation:** Upon successful payment, this server automatically generates a unique `LICENSE_KEY` (e.g., `RESTO-XXXX-YYYY`) and emails it to the customer along with the download link.\n- **Validation:** It acts as the central brain that tracks which license keys are valid, expired, or active on a specific computer.\n\n### 2. The Downloadable Desktop App (Offline)\nThis is your current React + Node.js code, but wrapped in **Electron** to become a `.exe` or `.dmg` file.\n- **Local Database:** Instead of connecting to a cloud database, we will package a local database (like SQLite or Local MongoDB) *inside* the app so the restaurant can use it 100% offline without the internet.\n- **License Lock:** The very first time the customer opens the app, it will show a \"Login / Enter License Key\" screen. It requires the internet *just for this step*. It checks the key against your Cloud Server. If valid, the software unlocks.\n- **Offline Mode:** Once unlocked, the restaurant can bill customers offline forever.\n\n### 3. The Auto-Update Server (Online)\n- We will host your app's installation files on a cloud service (like GitHub Releases or Amazon S3).\n- Whenever you add new features, you build a new version and upload it here.\n- When the restaurant's computer happens to be connected to the internet, their app will silently check this server, download 
<truncated 1218 bytes>
"# RestoPOS Business Model & Architecture\n\nThis plan outlines the complete architecture to turn your software into a **downloadable, sellable product** with offline capabilities, a central licensing server, and a powerful Super Admin Panel to run your entire software business.\n\n## Is it possible?\n**YES!** This is exactly how massive software companies operate. You are building an entire software ecosystem, not just a single app. \n\n## The Grand Architecture\n\nTo achieve your vision, we will build **three separate but connected systems**:\n\n### 1. The Super Admin Panel (Cloud Web App)\nThis is a secure, cloud-based dashboard **only for you and your staff**. You log into it via a website.\n- **License Management:** View every generated license key, see if it is active, expired, or blocked.\n- **Payment Tracking:** See all successful payments from clients who bought the software.\n- **Staff / Sales Agents:** Create \"Staff IDs\". When your sales staff go to a restaurant to install the software, they use their ID so you can track how many sales/downloads each staff member achieved.\n- **Referral System:** Track if an existing restaurant referred another restaurant, allowing you to give them discounts or rewards.\n- **Global Dashboard:** A master overview showing total revenue, total active software installs across the country, and daily activity.\n\n### 2. The Cloud Licensing Server (The \"Brain\")\nThis is the invisible internet server that powers the Super Admin Panel.\n- It automatically generates the `LICENSE_KEY` when a payment is made.\n- It constantly listens for requests from the offline desktop apps to verify if their license is still valid.\n\n### 3. The Downloadable Desktop App (Offline RestoPOS)\nThis is the React + Node.js software the restaurant owner downloads (`.exe` or `.dmg`).\n- **Offline Database:** It runs an internal database (SQLite/Local MongoDB) so the restaurant can bill 100% offline.\n- **License Lock:** When opened for the first time, it demands a License Key. It quickly checks the
<truncated 1569 bytes>
"# RestoPOS Business Model & Architecture\n\nThis plan outlines the complete architecture to turn your software into a **downloadable, sellable product** with offline capabilities, a central licensing server, and a powerful Super Admin Panel to run your entire software business.\n\n## Is it possible?\n**YES!** This is exactly how massive software companies operate. You are building an entire software ecosystem, not just a single app. \n\n## The Grand Architecture\n\nTo achieve your vision, we will build **three separate but connected systems**:\n\n### 1. The Super Admin Panel (Cloud Web App)\nThis is a secure, cloud-based dashboard **only for you and your staff**. You log into it via a website.\n- **License Management:** View every generated license key, see if it is active, expired, or blocked.\n- **Payment Tracking:** See all successful payments from clients who bought the software.\n- **Staff / Sales Agents:** Create \"Staff IDs\". When your sales staff go to a restaurant to install the software, they use their ID so you can track how many sales/downloads each staff member achieved.\n- **Referral System:** Track if an existing restaurant referred another restaurant, allowing you to give them discounts or rewards.\n- **Global Dashboard:** A master overview showing total revenue, total active software installs across the country, and daily activity.\n\n### 2. The Cloud Licensing Server (The \"Brain\")\nThis is the invisible internet server that powers the Super Admin Panel.\n- It automatically generates the `LICENSE_KEY` when a payment is made.\n- It securely communicates with the offline desktop apps to verify if their license is still valid.\n\n### 3. The Downloadable Desktop App (Offline RestoPOS)\nThis is the React + Node.js software the restaurant owner downloads (`.exe` or `.dmg`).\n- **Offline Database:** It runs an internal database (SQLite/Local MongoDB) so the restaurant can bill 100% offline.\n- **License Lock:** When opened for the first time, it demands a License Key. It quickly checks the internet 
<truncated 3844 bytes>
"# Petty Cash & Expenses Module\n\nThis plan outlines the architecture for building the new **Petty Cash & Expenses** module. Since this requires saving financial data, we will build a complete full-stack feature (Database -> API -> Frontend).\n\n## ⚠️ User Review Required\nPlease review the Expense Categories below to ensure they fit your restaurant's needs. We will include default categories like `Ingredients`, `Utility Bills`, `Staff Salary`, `Maintenance`, and `Miscellaneous`.\n\n## Proposed Changes\n\n### Backend (Database & APIs)\n\n#### [NEW] `Backend/models/Expense.js`\n- Create a MongoDB Schema to track expenses.\n- **Fields:** `amount` (Number), `description` (String), `category` (String), `paymentMode` (Cash/UPI/Bank), `date` (Date).\n- Add indexes on `date` and `category` for fast fetching.\n\n#### [NEW] `Backend/controllers/expenseController.js`\n- Functions to: `addExpense`, `getExpenses` (with date filtering), `deleteExpense`, and `getExpenseStats`.\n\n#### [NEW] `Backend/routes/expenseRoutes.js`\n- Map Express routes to the controller functions.\n\n#### [MODIFY] `Backend/server.js`\n- Register the new `/api/expenses` endpoint.\n\n---\n\n### Frontend (UI & Integration)\n\n#### [NEW] `Frontend/src/api/expenses.js`\n- Axios wrapper functions to communicate with the backend (`addExpense`, `fetchExpenses`).\n\n#### [NEW] `Frontend/src/components/Expenses.jsx`\n- A beautiful, modern UI similar to your active orders or menu management.\n- Features:\n  - **Summary Cards:** Total Expenses Today, Total This Week.\n  - **Add Expense Modal:** Quick entry form (Amount, Category, Description).\n  - **Expense List:** A clean table or list showing all recorded expenses.\n\n#### [MODIFY] `Frontend/src/App.jsx`\n- Add the new \"Petty Cash & Expenses\" tab to the sidebar navigation.\n- Order it appropriately (e.g., between Bill History and Dashboard).\n\n## Verification Plan\n- [ ] Ensure backend correctly connects and saves to the `expenses` collection.\n- [ ] Verify the frontend modal accurately adds an expens
<truncated 48 bytes>
"# Petty Cash & Expenses Module\n\nThis plan outlines the architecture for building the new **Petty Cash & Expenses** module. Since this requires saving financial data, we will build a complete full-stack feature (Database -> API -> Frontend).\n\n## ⚠️ User Review Required\nPlease review the Expense Categories below to ensure they fit your restaurant's needs. We will include default categories like `Ingredients`, `Utility Bills`, `Staff Salary`, `Maintenance`, and `Miscellaneous`.\n\n## Proposed Changes\n\n### Backend (Database & APIs)\n\n#### [NEW] `Backend/models/Expense.js`\n- Create a MongoDB Schema to track expenses.\n- **Fields:** `amount` (Number), `description` (String), `category` (String), `paymentMode` (Cash/UPI/Bank), `date` (Date).\n- Add indexes on `date` and `category` for fast fetching.\n\n#### [NEW] `Backend/controllers/expenseController.js`\n- Functions to: `addExpense`, `getExpenses` (with date filtering), `deleteExpense`, and `getExpenseStats`.\n\n#### [NEW] `Backend/routes/expenseRoutes.js`\n- Map Express routes to the controller functions.\n\n#### [MODIFY] `Backend/server.js`\n- Register the new `/api/expenses` endpoint.\n\n---\n\n### Frontend (UI & Integration)\n\n#### [NEW] `Frontend/src/api/expenses.js`\n- Axios wrapper functions to communicate with the backend (`addExpense`, `fetchExpenses`).\n\n#### [NEW] `Frontend/src/components/Expenses.jsx`\n- A beautiful, modern UI similar to your active orders or menu management.\n- Features:\n  - **Summary Cards:** Total Expenses Today, Total This Week.\n  - **Add Expense Modal:** Quick entry form (Amount, Category, Description).\n  - **Expense List:** A clean table or list showing all recorded expenses.\n\n#### [MODIFY] `Frontend/src/App.jsx`\n- Add the new \"Petty Cash & Expenses\" tab to the sidebar navigation.\n- Order it appropriately (e.g., between Bill History and Delivery Orders).\n\n## Verification Plan\n- [ ] Ensure backend correctly connects and saves to the `expenses` collection.\n- [ ] Verify the frontend modal accurately adds an 
<truncated 54 bytes>
"# Dashboard Enhancements Plan\n\nYou requested two excellent new features for the Dashboard to give you more granular control and deeper insights into your daily sales:\n1. **Item-Wise Sales:** To see exactly how many portions of a specific item (like Biriyani) were sold and the total revenue generated from it.\n2. **Recent Bills with Actions:** To view, edit, and delete today's recent bills directly from the dashboard without navigating to the Bill History tab.\n\nHere is the technical plan to implement these features smoothly.\n\n## Proposed Changes\n\n### Backend Updates\n\n#### [MODIFY] `Backend/controllers/billController.js`\n- **Top Items Aggregation:** We will update the `getDailyStats` API to aggregate the `items` array from all paid bills today. It will group items by name, calculate the total quantity sold, and the total revenue for each item, then sort them to find the top sellers.\n- **Recent Bills Query:** We will add a query inside `getDailyStats` to fetch the 5 most recent bills for today, including their full details so they can be viewed and managed from the dashboard.\n\n### Frontend Updates\n\n#### [MODIFY] `Frontend/src/components/Dashboard.jsx`\n- **Top Selling Items Section:** We will build a new UI section directly below the main stats cards. This will display a beautiful list of the top-selling items for the day, showing the item name, quantity sold, and total revenue generated.\n- **Recent Bills Section:** Next to or below the items, we will add a \"Recent Bills\" table. This table will display the latest transactions and will include:\n  - An **Eye icon** to view/print the bill.\n  - A **Trash icon** to delete the bill.\n  - (Note on Editing): Currently, \"editing\" a paid bill involves deleting it and recreating it, or we can add a specific flow to reopen a bill. We will ensure the delete and view flows match the Bill History tab perfectly.\n\n## Verification Plan\n- [ ] Ensure backend aggregation correctly calculates total quantities (e.g., if Biriyani is ordered 2 times in one bill a
<truncated 239 bytes>
"# Kitchen Order Ticket (KOT) Implementation Plan\n\nYou're completely right—a POS system is incomplete without a KOT! The kitchen staff needs a ticket that tells them what to cook, without showing any prices or financial details.\n\nHere is the plan to build and integrate the KOT system.\n\n## Proposed Changes\n\n### 1. New Component: `KOT.jsx`\n#### [NEW] `Frontend/src/components/KOT.jsx`\n- We will create a new printable component specifically designed for thermal printers in the kitchen.\n- **Details Included:** Table Number (e.g., \"Ground Floor - Table 1\"), Date, Time, Order Type (Dine-in/Takeaway).\n- **Items:** A clean list of items and their quantities.\n- **NO Prices:** Prices will be strictly hidden from the KOT, as is standard practice for kitchen staff.\n\n### 2. Update Billing Page\n#### [MODIFY] `Frontend/src/components/BillingPage.jsx`\n- We will add a **KOT** button next to the `PRINT` and `SETTLE` buttons in the bottom right corner of the active order panel.\n- Clicking the KOT button will temporarily save the current order state and immediately open the KOT printable modal.\n\n## User Review Required\n> [!IMPORTANT]\n> The KOT will pop up exactly like the Invoice, allowing you to hit \"Print\" and send it to your thermal kitchen printer. Does this workflow sound perfect for your restaurant operations?"
"# Delta KOT Implementation Plan\n\nCurrently, the KOT system blindly prints the entire cart every time the KOT button is pressed. To prevent duplicate orders being sent to the kitchen, we need to track which items have already been printed.\n\n## Proposed Changes\n\n### 1. Backend Schema Update\n#### [MODIFY] `Backend/models/Bill.js`\n- Add `printedQuantity: { type: Number, default: 0 }` to the `items` array schema. This will track how many of each item have already been sent to the kitchen.\n\n### 2. Backend API Endpoint\n#### [MODIFY] `Backend/routes/billing.js` & `Backend/controllers/billController.js`\n- Create a new endpoint `POST /api/billing/:id/kot`.\n- This endpoint will simply iterate over all items in the specified bill and update `printedQuantity = quantity`.\n\n### 3. Frontend API Service\n#### [MODIFY] `Frontend/src/api/billing.js`\n- Add `markKOTPrinted(id)` function to call the new backend endpoint.\n\n### 4. Frontend Logic\n#### [MODIFY] `Frontend/src/components/BillingPage.jsx`\n- Update `handlePrintKOT()` logic:\n  1. Calculate the \"delta\" (the difference between the current `cart` quantity and the `printedQuantity` from the database).\n  2. If an item has `quantity > printedQuantity`, it will appear on the KOT.\n  3. If an item has `quantity < printedQuantity` (meaning a waiter removed an item *after* it was sent to the kitchen), it will appear with a negative quantity (e.g., `-1 CHICKEN 65`) to alert the kitchen to stop cooking it.\n  4. Automatically save the order to the database, call the new `markKOTPrinted` endpoint, and then display the printable KOT with *only* the delta items.\n\n## User Review Required\n> [!IMPORTANT]\n> If a waiter adds 2 items, prints a KOT, and then realizes they made a mistake and removes 1 item, the system will print a new KOT with a quantity of `-1` for that item. This acts as a cancellation ticket so the chef knows to stop cooking it. Does this logic work perfectly for your restaurant?"
"# KOT History Grid UI Redesign Plan\n\nCurrently, the KOT History uses large cards for every KOT. As you pointed out, this format is very bulky and lengthy when dealing with a high volume of orders.\n\nWe will completely rewrite `KOTHistory.jsx` to use a compact data table (grid) format, similar to the `Bill History` page. \n\n## User Review Required\n\nPlease review the proposed column layout for the new KOT History table.\n- **KOT Number** (e.g., KOT-1, CANCEL-1)\n- **Time** (e.g., 09:23 AM)\n- **Table / Source** (e.g., Ground Floor - Table 1)\n- **Items Summary** (A condensed view like `2x Chicken, 1x Soup...` instead of a massive list)\n- **Action** (A `View/Reprint` button that opens the full KOT slip pop-up)\n\n## Proposed Changes\n\n### Backend Components\n\n#### [MODIFY] [billController.js](file:///Users/apple/Bilings/restaurat-billing/Restaurant_Billing_System/Backend/controllers/billController.js)\n- Update the `getTodayKOTs` controller to accept `date` and `search` query parameters.\n- If `date` is provided, fetch KOTs from that specific date. If not, default to today.\n- If `search` is provided, filter the results by matching `kotNumber` or `tableNo`.\n\n### Frontend Components\n\n#### [MODIFY] [billing.js API](file:///Users/apple/Bilings/restaurat-billing/Restaurant_Billing_System/Frontend/src/api/billing.js)\n- Update `apiGetTodayKOTs` to accept `(date, search)` parameters and pass them to the backend API endpoint.\n\n#### [MODIFY] [KOTHistory.jsx](file:///Users/apple/Bilings/restaurat-billing/Restaurant_Billing_System/Frontend/src/components/KOTHistory.jsx)\n- **UI Complete Rewrite**: Remove the card grid and replace it with an HTML `<table>` layout.\n- **Search Bar**: Implement a debounced search input at the top of the page.\n- **Date Filter**: Add a standard HTML `<input type=\"date\">` picker at the top of the page to allow selecting historic dates.\n- **State Management**: Add `searchTerm`, `selectedDate`, and `debouncedSearchTerm` states to re-trigger data fetching automatically.\n\n## Verif
<truncated 264 bytes>
"# Profile Editing Implementation Plan\n\nCurrently, the user's name (`narasimha`) is created during the initial setup and cannot be modified from the UI. You are completely right to ask for this, as it should be easily editable!\n\nTo fix this, I will add a **\"Profile Settings\"** section directly into your existing `Settings` page.\n\n## Proposed Changes\n\n### Backend Updates\n1. **[NEW] `PUT /api/auth/profile` Endpoint**: I will create a new API route in `authRoutes.js` and `authController.js` that allows the currently logged-in user to securely update their `username`.\n\n### Frontend Updates\n1. **[MODIFY] `Settings.jsx`**: I will add a new **\"Profile Information\"** card on the Settings page, sitting right next to your \"Restaurant Information\" card. It will have an input field for your Username.\n2. **[MODIFY] `auth.js` API**: I will add an `updateProfile` function to call the new backend endpoint.\n3. **[MODIFY] `App.jsx` & LocalStorage**: When the username is updated, the frontend will automatically sync and display the new name everywhere on the screen instantly.\n\nPlease review this plan. If you approve, I will add this new settings card for you!"
"# Implementation Plan: Split Payments & Rounding\n\nTo properly support \"Mixed\" payments and round your bills to the nearest rupee, I will make the following changes across the stack:\n\n## Proposed Changes\n\n### 1. Mathematical Rounding (Frontend)\nCurrently, the backend perfectly rounds the final bill (e.g. `81.60` -> `82`), but the frontend calculates the exact decimal before the bill is generated. \n- **[MODIFY] `BillingPage.jsx`**: I will wrap the `total` calculation in `Math.round()` so that the screen perfectly matches the backend and only asks the customer for the exact rounded figure.\n\n### 2. Backend Schema Update\nTo correctly track \"Mixed\" payments for your analytics and daily stats, the database needs to know exactly how much came from each source.\n- **[MODIFY] `models/Bill.js`**: Add a new `splitPayments` object to the database schema to store `cash`, `upi`, and `card` amounts separately.\n- **[MODIFY] `controllers/billController.js`**: Update the `settleBill` endpoint to securely accept and save the `splitPayments` amounts.\n\n### 3. Payment Modal (Mixed Mode UI)\n- **[MODIFY] `PaymentModal.jsx`**: \n  - When you click \"Mixed\", you will see three dedicated input boxes: Cash, UPI, and Card.\n  - **Validation logic**: The system will live-calculate the sum of these three inputs. \n  - To prevent over-billing or under-billing, the \"Complete Payment\" button will only unlock when `Cash + UPI + Card` is exactly equal to the rounded bill total.\n\nPlease review this plan. If this logic fits your exact needs, give me the approval and I will implement it immediately!"
